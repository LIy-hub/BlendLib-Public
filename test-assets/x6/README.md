# X6 Test Assets

This directory contains human-readable, non-runtime fixture inputs for the isolated X6 candidate.
They are not discovered by the v1 descriptor loader and do not enable an X6 resource binding.

`variant-manifest-v1.txt` mirrors the client test resource and documents the six-variant manifest
shape used by the deterministic prepare-time parser.
