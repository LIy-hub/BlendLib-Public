# PlatformAdapter example

This package shows a controlled X4-style adapter which returns only a stable RegistrationReceipt.
It must be installed and later uninstalled by one version-specific platform lifecycle owner. A
normal consumer must not call PlatformAdapterControl global installation methods.
