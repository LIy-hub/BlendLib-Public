# RenderBackendProvider example

This package offers a public-standard backend capability at current protocol 1.1.0 and priority
20, with an explicit CPU standard fallback. It is metadata and lifecycle code only: it never
creates a backend during submit and contains no graphics API import.
