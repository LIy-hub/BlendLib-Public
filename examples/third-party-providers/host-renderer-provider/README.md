# HostRendererProvider example

This package declares entity, block-entity, and item semantic host-kind compatibility at current
protocol 1.1.0 and priority 10. It intentionally exposes no platform renderer object.
