# MaterialProvider example

This package offers a standard opaque material capability at current protocol 1.1.0 and priority
30. It exposes an explicit optional fallback to the same standard opaque route, never a custom
shader or raw graphics handle. A host must independently prove fallback equivalence before publish.
