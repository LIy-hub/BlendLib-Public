package com.liy.blendlib.fabric.client.render;

/** The six X6 variant effects. Every effect is resolved before render submit. */
public enum X6VariantKind {
    SKIN,
    MATERIAL,
    MESH,
    EQUIPMENT,
    DAMAGE_STAGE,
    PART_VISIBILITY
}
