package com.liy.blendlib.fabric.client.render;

/** Explicit blend semantics selected only from a verified standard material path. */
public enum X6BlendSemantic {
    OPAQUE,
    CUTOUT,
    TRANSLUCENT,
    INHERIT,
    UNSUPPORTED
}
