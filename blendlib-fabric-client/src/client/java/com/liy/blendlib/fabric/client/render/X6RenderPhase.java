package com.liy.blendlib.fabric.client.render;

/** Ordered X6 presentation phases. Ordering is explicit and independent of registration insertion. */
public enum X6RenderPhase {
    BASE,
    POST_BASE,
    OVERLAY,
    ATTACHMENT,
    PRESENTATION
}
