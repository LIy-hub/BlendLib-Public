package com.liy.blendlib.fabric.client.render;

/** Texture source semantic recorded before submit; no submit-time resource lookup is allowed. */
public enum X6TextureTarget {
    BASE_TEXTURE,
    SECONDARY_TEXTURE,
    PART_TEXTURE,
    ATTACHMENT_SNAPSHOT,
    NONE
}
