package com.liy.blendlib.fabric.client.render;

/** Explicit packed-light target for a prepared X6 layer. */
public enum X6LightSemantic {
    PACKED_LIGHT,
    FULL_BRIGHT,
    INHERIT,
    HOST_MANAGED,
    UNSUPPORTED
}
