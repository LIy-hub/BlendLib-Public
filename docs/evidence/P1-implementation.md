# P1 Implementation Evidence

Phase: P1
Commit: feat(p1): establish modular Fabric skeleton (local focused commit; exact hash is recorded by Git history).
Files: Gradle 9.6.0 wrapper; Java 25 multi-project skeleton; API/core/common/client/showcase/consumer-fixture modules; P1 boundary tests.
Automated tests: PASS - `$env:JAVA_HOME='C:\Program Files\Java\latest\jdk-25'; .\gradlew.bat clean check` and `.\gradlew.bat :blendlib-core:test` completed successfully. The `check` graph executes API/core/common/showcase source-boundary tests and the consumer fixture test.
Compiled boundary scan: PASS - compiled API/core/common/showcase classes contain no forbidden API/core platform references, common client reference, or showcase non-public BlendLib reference.
Build: PASS - `$env:JAVA_HOME='C:\Program Files\Java\latest\jdk-25'; .\gradlew.bat buildRelease` completed successfully and includes all P1 checks plus client/showcase `remapJar` entries.
Reviewer remediation: PASS - Showcase now declares `:blendlib-fabric-client` as `runtimeOnly`, not `implementation`. Its `verifyShowcaseDependencyBoundary` task runs from `check` and rejects any non-API BlendLib project or published module on `compileClasspath`, while requiring api/core/common/client on `runtimeClasspath` for the isolated development run. After the repair, `:blendlib-showcase:check`, `:blendlib-api-consumer-fixture:check`, `buildRelease`, and `:blendlib-showcase:dependencies --configuration compileClasspath` all completed successfully; the reported compile classpath contains `project ':blendlib-api'` but not fabric-client/core/common.
Post-remediation dedicated server: PASS for the P1 `Done` gate. After confirming no old Showcase server and no `25565` listener, an independent test runner ran `$env:JAVA_HOME='C:\\Program Files\\Java\\latest\\jdk-25'; .\\gradlew.bat :blendlib-showcase:runServer` exactly once. Its new `D:\\BlendLib\\blendlib-showcase\\run\\server\\logs\\latest.log:1` identifies Minecraft 26.1.2 and Loader 0.19.3; `:65` starts the server, `:69` binds `*:25565`, `:74` records `Done (0.466s)!`, and `:81` records all dimensions saved. The same log has zero `ERROR`, `NoClassDefFoundError`, `ClassNotFoundException`, and `net.minecraft.client` matches. The post-cleanup SHA-256 is `69C02AD02633FE4BD2482851D285E8B2B2ED9C2AF54615B8266BBB544023E837`. The host again closed stdin, so after the completed save the coordinator exact-command-line-verified and terminated only the isolated `D:\\BlendLib` server child to release the port; no official server or world was accessed. The earlier `Done` evidence below is retained as pre-remediation history and is not reused as proof for this repair.
Dedicated server: PASS for the P1 `Done` gate. An independent test runner executed `$env:JAVA_HOME='C:\\Program Files\\Java\\latest\\jdk-25'; .\\gradlew.bat :blendlib-showcase:runServer` exactly once. The isolated run directory was `D:\\BlendLib\\blendlib-showcase\\run\\server`, with `eula.txt` equal to `eula=true`; `logs\\latest.log:84` records Minecraft 26.1.2 startup, `:88` the `*:25565` listener, `:94` `Done (2.628s)!`, and `:95-101` completed all-dimension saves. The log SHA-256 after cleanup is `6BE1F6EA1533A922FA7EC05DE3C1E1ACCC6F7B8F7A2C245ADBE825F3CACB1715`. The non-interactive host closed the launched task's stdin, so `stop` could not be sent through that same session and no normal Gradle exit code exists. After the completed save and an exact command-line check, the coordinator terminated only the isolated `D:\\BlendLib` server child to release the port. No official server or world was accessed. This cleanup limitation is retained as evidence rather than treated as a substitute for the required `Done` line.
Task discovery: PASS - `:blendlib-showcase:tasks --all` lists `runClient` and `runServer` as the named Showcase client/server entries.
Client/manual: WAITING. No client visual verification is claimed for this alpha skeleton.
Performance: Not applicable to P1.
Known gaps: GLB loading, assets, rendering, networking, and all P2+ behavior are intentionally absent. Loom 1.15.5 treats Minecraft 26.1.2 as an already-Mojang-named, non-obfuscated environment, so a second `officialMojangMappings()` layer is rejected; the required client/showcase `remapJar` task names are compatibility aliases to their standard jars.
Gate: PASS - independent p1_reviewer accepted the full P1 diff, remediation, automated checks, and post-remediation isolated `Done` evidence.
Next phase: Create the focused local P1 commit, then begin P2.
