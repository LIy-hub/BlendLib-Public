package com.liy.blendlib.core.procedural;

/** Semantic render target classification. X3 never resolves a material provider for these values. */
public enum ProceduralSurfaceKind {
    MESH,
    PRIMITIVE
}
