package com.liy.blendlib.core.procedural;

/** Stable internal X3 codes for bounded, structured diagnostics. */
public enum ProceduralDiagnosticCode {
    INVALID_TARGET,
    DUPLICATE_ID,
    HIERARCHY_CYCLE,
    CARDINALITY_MISMATCH,
    STALE_SCOPE,
    STALE_REVISION,
    OWNER_THREAD_REJECTED,
    NONFINITE_INPUT,
    BOUNDS_REJECTED,
    COMMAND_OVERFLOW,
    DIAGNOSTIC_OVERFLOW,
    HOOK_DUPLICATE,
    HOOK_CONFLICT,
    HOOK_FAILURE,
    SOCKET_MISSING,
    SOCKET_SCOPE_MISMATCH,
    HIDDEN_SOCKET,
    ATTACHMENT_TARGET_MISSING,
    ATTACHMENT_HIDDEN_TARGET,
    ATTACHMENT_LIMIT,
    ATTACHMENT_DEPTH,
    ATTACHMENT_CYCLE,
    ATTACHMENT_GRAPH_RETIRED,
    IK_FAILURE,
    VISUAL_EVENT_REJECTED,
    VISUAL_EVENT_DEDUPLICATED,
    VISUAL_EVENT_CONFLICT,
    VISUAL_EVENT_OVERFLOW,
    VISUAL_EVENT_PROVENANCE,
    VISUAL_EVENT_REPLAYED,
    VISUAL_EVENT_REPLAY_FENCE_OVERFLOW
}
